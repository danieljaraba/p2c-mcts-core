version: '3.8'

services:
  mcts-core:
    build: .
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - ./src:/app/src
    restart: unless-stopped
